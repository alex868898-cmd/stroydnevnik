# Application settings
PORT=3001
HOST=127.0.0.1
NODE_ENV=development

# Storage settings
# STORAGE_ROOT_PATH=./data

# NOTE: Provider-specific environment variables (e.g. API keys for Kling, Luma, Runway, etc.)
# and database credentials (if any) will be added here after the choices in Q-1 and Q-3 
# (real provider and final stack/ORM) are officially confirmed.
